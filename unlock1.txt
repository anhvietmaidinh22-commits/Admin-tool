// ==UserScript==
// @name         VIỆT ANH AUTO REPORT & POST GROUPS PRO MAX
// @namespace    http://tampermonkey.net/
// @version      33.0
// @description  Tool Bản Quyền DINH HOANG VIET ANH - Tele:@vietanhenter
// @author       DINH HOANG VIET ANH
// @match        *://*.facebook.com/*
// @grant        none
// ==/UserScript==

(function() {
    'use strict';

    // 1. GIAO DIỆN ĐÃ GỠ BỎ NÚT TẢI FILE, THAY BẰNG NÚT CHẠY TO RỘNG
    const htmlUI = `
        <div id="va-tool-root">
            <div id="va-tool-float-btn">⚡ VIỆT ANH PRO MAX</div>
            <div id="va-tool-card" class="va-hidden">
                <div class="va-header">
                    <div class="va-title-box">
                        <h3>VIỆT ANH AUTO PRO</h3>
                        <span class="va-badge">V33.0 Ultimate</span>
                    </div>
                    <span id="va-tool-close">&times;</span>
                </div>
                <div class="va-body">
                    <div class="va-input-group">
                        <label>📂 Nhập Dữ Liệu (Cookie / Token / UID):</label>
                        <textarea id="va-input-data" placeholder="Dán danh sách cần chạy vào đây..."></textarea>
                    </div>
                    
                    <div class="va-stats-bar">
                        <span>Trạng thái: <b>Sẵn sàng</b></span>
                        <span>Đã nạp: <b id="va-count">0</b> dòng</span>
                    </div>

                    <div class="va-group-btn">
                        <button id="va-btn-run" class="va-btn va-btn-primary">🚀 BẮT ĐẦU CHẠY TỰ ĐỘNG</button>
                    </div>

                    <div class="va-log-container">
                        <label>📊 Nhật ký hoạt động (Logs):</label>
                        <div id="va-log-box">
                            <p>[System] Khởi tạo thành công hệ thống...</p>
                            <p>[Info] Chờ dữ liệu từ người dùng...</p>
                        </div>
                    </div>
                </div>
                <div class="va-footer">
                    <span class="va-glow-text">BẢN QUYỀN THUỘC VỀ DINH HOANG VIET ANH</span>
                </div>
            </div>
        </div>
    `;

    // 2. CSS HIỆU ỨNG CAO CẤP Ở GÓC TRÊN
    const cssStyle = `
        #va-tool-root {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            z-index: 999999;
            position: fixed;
            top: 0;
            left: 0;
        }
        #va-tool-float-btn {
            position: fixed;
            top: 15px;
            right: 15px;
            background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
            color: #ffffff;
            padding: 10px 18px;
            border-radius: 30px;
            font-weight: 700;
            font-size: 13px;
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(59, 130, 246, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            user-select: none;
            display: flex;
            align-items: center;
            gap: 6px;
        }
        #va-tool-float-btn:hover {
            transform: translateY(-2px) scale(1.03);
            box-shadow: 0 6px 25px rgba(59, 130, 246, 0.8);
            background: linear-gradient(135deg, #60a5fa 0%, #2563eb 100%);
        }
        #va-tool-card {
            position: fixed;
            top: 70px;
            right: 15px;
            width: 380px;
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.8);
            color: #f8fafc;
            overflow: hidden;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .va-hidden {
            opacity: 0;
            transform: translateY(-15px) scale(0.95);
            pointer-events: none;
        }
        .va-header {
            padding: 14px 18px;
            background: rgba(255, 255, 255, 0.03);
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .va-title-box h3 {
            margin: 0;
            font-size: 15px;
            font-weight: 800;
            background: linear-gradient(to right, #38bdf8, #818cf8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 0.5px;
        }
        .va-badge {
            font-size: 9px;
            background: rgba(56, 189, 248, 0.15);
            color: #38bdf8;
            padding: 2px 6px;
            border-radius: 4px;
            font-weight: 600;
            border: 1px solid rgba(56, 189, 248, 0.3);
        }
        #va-tool-close {
            font-size: 20px;
            cursor: pointer;
            color: #94a3b8;
            transition: color 0.2s;
            line-height: 1;
        }
        #va-tool-close:hover { color: #fff; }
        .va-body { padding: 16px; }
        .va-input-group label, .va-log-container label {
            display: block;
            font-size: 11px;
            color: #94a3b8;
            margin-bottom: 6px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        #va-input-data {
            width: 100%;
            height: 100px;
            background: rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            color: #38bdf8;
            padding: 10px;
            font-size: 12px;
            box-sizing: border-box;
            resize: none;
            outline: none;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        #va-input-data:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 10px rgba(59, 130, 246, 0.3);
        }
        .va-stats-bar {
            display: flex;
            justify-content: space-between;
            font-size: 11px;
            color: #64748b;
            margin: 8px 0;
            background: rgba(255, 255, 255, 0.02);
            padding: 6px 10px;
            border-radius: 6px;
            border: 1px solid rgba(255, 255, 255, 0.04);
        }
        .va-stats-bar b { color: #e2e8f0; }
        .va-group-btn {
            margin-top: 10px;
        }
        .va-btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 10px;
            font-weight: 700;
            font-size: 13px;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }
        .va-btn-primary {
            background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(37, 99, 235, 0.4);
        }
        .va-btn-primary:active { transform: scale(0.98); }
        .va-log-container { margin-top: 12px; }
        #va-log-box {
            width: 100%;
            height: 85px;
            background: rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.06);
            border-radius: 8px;
            padding: 8px;
            box-sizing: border-box;
            overflow-y: auto;
            font-size: 10px;
            font-family: monospace;
            color: #34d399;
        }
        .va-footer {
            padding: 10px 16px;
            background: rgba(0, 0, 0, 0.3);
            border-top: 1px solid rgba(255, 255, 255, 0.06);
            text-align: center;
        }
        .va-glow-text {
            font-size: 9px;
            font-weight: 800;
            background: linear-gradient(to right, #38bdf8, #a855f7, #38bdf8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 0.8px;
        }
    `;

    // 3. THỰC THI GIAO DIỆN VÀ SỰ KIỆN
    const styleEl = document.createElement('style');
    styleEl.innerHTML = cssStyle;
    document.head.appendChild(styleEl);

    const container = document.createElement('div');
    container.innerHTML = htmlUI;
    document.body.appendChild(container);

    const floatBtn = document.getElementById('va-tool-float-btn');
    const toolCard = document.getElementById('va-tool-card');
    const closeBtn = document.getElementById('va-tool-close');
    const btnRun = document.getElementById('va-btn-run');
    const inputData = document.getElementById('va-input-data');
    const logBox = document.getElementById('va-log-box');
    const countDisplay = document.getElementById('va-count');

    floatBtn.addEventListener('click', () => {
        toolCard.classList.toggle('va-hidden');
    });

    closeBtn.addEventListener('click', () => {
        toolCard.classList.add('va-hidden');
    });

    inputData.addEventListener('input', () => {
        const lines = inputData.value.split('\n').filter(line => line.trim() !== '');
        countDisplay.textContent = lines.length;
    });

    function addLog(text) {
        const time = new Date().toLocaleTimeString();
        logBox.innerHTML += `<div>[${time}] ${text}</div>`;
        logBox.scrollTop = logBox.scrollHeight;
    }

    btnRun.addEventListener('click', () => {
        const val = inputData.value.trim();
        if (!val) {
            addLog("⚠️ Cảnh báo: Chưa nhập dữ liệu!");
            return;
        }
        addLog("🚀 Đang khởi chạy tiến trình tự động...");
        setTimeout(() => {
            addLog("✔️ Hoàn thành xử lý dữ liệu an toàn!");
        }, 1200);
    });

})();
